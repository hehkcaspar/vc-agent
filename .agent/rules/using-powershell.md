---
trigger: always_on
---

Rule: The command-line interface for this workspace is strictly Windows 11 using PowerShell. All generated commands, file paths (e.g., using \ instead of /), and scripts must be written in valid PowerShell syntax. Do not assume a Linux/Bash environment or output Bash-specific commands (like export or grep) unless explicitly instructed.